/**
 * Omnibox section module registry (metadata). Routing lives in Omnibox.svelte.
 * List row UI is shared via OmniboxSectionResultList / section wrappers.
 */
export {
    SECTIONS,
    PLACEHOLDER_SECTION_IDS,
    DEFAULT_SECTION_ID,
    DEFAULT_SECTION_INDEX,
} from "./omniboxSections.js";
